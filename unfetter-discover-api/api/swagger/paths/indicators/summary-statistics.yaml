x-swagger-router-controller: indicators
get:
  tags:  
  - STIX-Indicators
  description: Generates summary statistics for the Analytic Exchange
  operationId: summaryStatistics
  produces: 
  - application/vnd.api+json  
  responses:
    "200":
      description: Success
      schema:
        type: object
    default:
      description: Error
      schema:
        $ref: "#/definitions/ErrorJsonApi"