/**
 * @description attack pattern as defined coming from the unfetter database
 */
export class AttackPattern {
    public readonly type = 'attack-pattern';
    public id: string;
    public attributes = { } as {[key: string]: string};
}
